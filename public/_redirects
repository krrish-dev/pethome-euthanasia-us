# Netlify redirects intentionally disabled.
# Netlify's built-in Pretty URLs can serve Astro file-format pages such as /services from /services.html.
# Keep canonical/internal URL normalization in the Astro build, but do not force URL redirects here to avoid redirect loops.
