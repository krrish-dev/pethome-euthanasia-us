# Clean canonical URL redirects for static hosts that support Netlify/Cloudflare Pages syntax.
# The site canonical policy is: no .html extension and no trailing slash except homepage.

/index.html / 301!
/*.html /:splat 301!
/*/ /:splat 301!
